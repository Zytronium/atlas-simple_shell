#!/bin/bash
if test -f ./mix_ls_bin_ls_spaces.bash
then
  cat mix_ls_bin_ls_spaces.bash
fi

#note: We only plan to use this if we are told that it's allowed. This script
# would print the contents of a specific checker script, to help us see what
# exactly it's doing, to help us debug.
